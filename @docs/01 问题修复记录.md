### Fix Record - 2025-01-27

-   **Problem Description:** Jupyter Notebook 中的 Markdown 表格格式混乱，导致表格无法正常显示。表格存在以下问题：
    1. 表头行包含 HTML 标签和换行符，格式不规范
    2. 分隔符行末尾有多余的 `||`
    3. 部分数据行缺少最后的 `|` 分隔符
    4. 部分数据行末尾有多余的 `||`
    5. 列数不一致，有些行有4列，有些行只有2列

-   **Root Cause:** Markdown 表格格式不规范，列数不统一，分隔符使用错误。

-   **Solution:** 
    1. 简化表头，移除 HTML 标签，使用标准的 Markdown 表格格式
    2. 修正分隔符行为标准的 `|:---|:---|:---|` 格式
    3. 统一所有数据行为3列格式（符号、描述、Python）
    4. 确保每行都以 `|` 结尾，移除多余的 `||`
    5. 清理多余的空格和格式问题

-   **Impact Scope:**
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/C1_W1_Lab03_Model_Representation_Soln.ipynb` (Cell 2)

---

### Fix Record - 2025-01-27

-   **Problem Description:** Jupyter Notebook 中使用了 `%matplotlib widget` 魔法命令，该命令需要安装 `ipympl` 包，且在某些环境中可能不支持，导致代码无法正常运行。

-   **Root Cause:** `%matplotlib widget` 需要额外的依赖包 `ipympl`，而且不是所有 Jupyter 环境都支持该命令。相比之下，其他实验文件（Lab03、Lab05）都使用 `%matplotlib inline`，这是更通用和兼容的选择。

-   **Solution:** 
    将 `%matplotlib widget` 改为 `%matplotlib inline`，这样可以确保代码在所有标准的 Jupyter 环境中都能正常运行，无需额外安装依赖包。

-   **Impact Scope:**
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/C1_W1_Lab04_Cost_function_Soln.ipynb` (Cell 3)

---

### Fix Record - 2025-01-27

-   **Problem Description:** 直接运行 `lab_utils_uni.py` 或 `lab_utils_common.py` 文件时，出现 `FileNotFoundError`，无法找到 `./deeplearning.mplstyle` 样式文件。这是因为使用了相对路径，当工作目录不是文件所在目录时，相对路径无法正确解析。

-   **Root Cause:** 代码中使用了相对路径 `./deeplearning.mplstyle`，该路径依赖于当前工作目录。当直接运行 Python 文件时，工作目录可能不是文件所在的目录，导致找不到样式文件。

-   **Solution:** 
    1. 使用 `os.path.dirname(os.path.abspath(__file__))` 获取当前文件所在的目录
    2. 使用 `os.path.join()` 构建样式文件的绝对路径
    3. 这样无论从哪个目录运行文件，都能正确找到样式文件

-   **Impact Scope:**
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/lab_utils_common.py` (第9行)
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/lab_utils_uni.py` (第14行)

---

### Fix Record - 2025-01-27

-   **Problem Description:** 运行 `plt_stationary` 和 `plt_update_onclick` 代码后，无法通过点击等高线图来选择点并更新图形。这是因为使用了 `%matplotlib inline` 静态后端，不支持交互式事件处理。

-   **Root Cause:** `plt_update_onclick` 类依赖于 matplotlib 的事件处理系统（`button_press_event`），但 `%matplotlib inline` 是静态后端，不支持交互式事件。之前为了兼容性将 `%matplotlib widget` 改为了 `%matplotlib inline`，导致交互功能失效。

-   **Solution:** 
    1. 在 Cell 16 添加了说明，告知用户交互功能需要交互式后端，并提供了启用步骤
    2. 在 Cell 17 添加了后端检测代码，当使用 `inline` 后端时会显示警告和启用交互功能的说明
    3. 如果用户需要交互功能，可以安装 `ipympl` 并将 `%matplotlib inline` 改为 `%matplotlib widget`

-   **Impact Scope:**
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/C1_W1_Lab04_Cost_function_Soln.ipynb` (Cell 16, Cell 17)

---

### Fix Record - 2025-01-27

-   **Problem Description:** `C1_W1_Lab05_Gradient_Descent_Soln.ipynb` 笔记本中有大量英文内容尚未翻译成中文，包括梯度下降的说明、实现指南、可视化说明等。

-   **Root Cause:** 该笔记本在之前的翻译过程中可能被遗漏，或者部分内容在后续更新中未翻译。

-   **Solution:** 
    将笔记本中所有英文内容翻译成中文，包括：
    1. Cell 9: 梯度下降的数学公式和说明
    2. Cell 10: 实现梯度下降的指南
    3. Cell 14: 关于梯度计算的说明
    4. Cell 16: 关于梯度图的说明
    5. Cell 17: 梯度下降实现说明
    6. Cell 20: 梯度下降过程特征说明
    7. Cell 21: 代价与迭代次数关系的说明
    8. Cell 23: 预测部分的说明
    9. Cell 25: 绘图部分的说明
    10. Cell 27: 等高线图的说明
    11. Cell 28: 放大视图的说明
    12. Cell 30: 增加学习率部分的说明
    13. Cell 32: 学习率过大的说明
    14. Cell 34: 发散图的说明
    15. Cell 35: 恭喜部分的总结

-   **Impact Scope:**
    -   `C1 - Supervised Machine Learning - Regression and Classification/week1/Optional Labs/C1_W1_Lab05_Gradient_Descent_Soln.ipynb` (多个单元格)

---


